export const InfoBox = ({ title, children }) => (
  <div className="callout">
    <strong style={{ color: '#C57A33' }}>{title}</strong>
    <div style={{ marginTop: '0.5rem' }}>{children}</div>
  </div>
)